{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "serverUrl": {
      "type": "string",
      "format": "uri",
      "description": "The MCP server SSE URL"
    },
    "authType": {
      "type": "string",
      "enum": ["oauth", "bearerToken"],
      "default": "oauth"
    },
    "bearerToken": {
      "type": "string",
      "writeOnly": true,
      "description": "Bearer token value (required when authType is 'bearerToken')",
      "dependencies": {
        "authType": {
          "const": "bearerToken"
        }
      }
    },
    "customHeaders": {
      "type": "array",
      "description": "Custom HTTP headers to include in MCP server requests",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1
          },
          "value": {
            "type": "string"
          }
        },
        "required": ["name", "value"],
        "additionalProperties": false
      }
    }
  },
  "required": ["serverUrl", "authType"],
  "allOf": [
    {
      "if": {
        "properties": {
          "authType": {
            "const": "bearerToken"
          }
        }
      },
      "then": {
        "required": ["serverUrl", "authType", "bearerToken"]
      }
    },
    {
      "if": {
        "properties": {
          "authType": {
            "const": "oauth"
          }
        }
      },
      "then": {
        "not": {
          "required": ["bearerToken"]
        }
      }
    }
  ],
  "additionalProperties": false
}